//create a database named BakeryShop

test> use BakeryShop
switched to db BakeryShop

//create a collection named Orders with sample order document

BakeryShop> db.Orders.insert({customerName: "Alice Johnson",item: "Chocolate Cake",quantity: 2, totalPrice: 500})
DeprecationWarning: Collection.insert() is deprecated. Use insertOne, insertMany, or bulkWrite.
{
  acknowledged: true,
  insertedIds: { '0': ObjectId('68cc2627c064fe0d42cebea4') }
}

//showing the collection exist

BakeryShop> db.Orders.find()
[
  {
    _id: ObjectId('68cc2627c064fe0d42cebea4'),
    customerName: 'Alice Johnson',
    item: 'Chocolate Cake',
    quantity: 2,
    totalPrice: 500
  }
]

// Drop the orders collection

BakeryShop> db.Orders.drop()
true
BakeryShop> show collections

//Drop the BakeryShop database

BakeryShop> db.dropDatabase()
{ ok: 1, dropped: 'BakeryShop' }
BakeryShop> show dbs
admin   40.00 KiB
config  72.00 KiB
local   64.00 KiB
